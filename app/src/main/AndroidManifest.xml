<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.study.carrotmarket">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".setting.activity.SettingActivity"/>
        <activity android:name=".setting.activity.KeyWordNotifyActivity" />
        <activity android:name=".setting.activity.CheckInActivity" />
        <activity android:name=".setting.activity.RegionSettingActivity" />
        <activity android:name=".setting.activity.WebViewActivity" />
        <activity android:name=".setting.activity.FollowingArticleActivity" />
        <activity android:name=".setting.activity.UserStoryArticlesActivity" />
        <activity android:name=".setting.activity.InterestsActivity" />
        <activity android:name=".setting.activity.FavoriteActivity" />
        <activity android:name=".setting.activity.PurchaseActivity" />
        <activity android:name=".setting.activity.ProfileActivity" />
        <activity android:name=".setting.activity.SalesActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>